# Backend origin, without the /api prefix — the API client appends it.
# Required for `next build`; `next dev` falls back to http://localhost:5000.
NEXT_PUBLIC_API_URL=http://localhost:5000

# Must match PAYMENT_EXPIRY_MINUTES in the backend.
NEXT_PUBLIC_PAYMENT_EXPIRY_MINUTES=30
