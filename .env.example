# Backend origin, without /api. Must match the running backend's PORT.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Keep this aligned with backend IMAGE_MAX_MB. Backend limits are authoritative.
NEXT_PUBLIC_EVENT_IMAGE_MAX_MB=2
